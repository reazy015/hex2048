export class Hex {
    constructor(public x: number, public z: number, public y: number, public value: number) {
        this.x = x;
        this.y = y;
        this.z = z;
        this.value = value;
    }
}
